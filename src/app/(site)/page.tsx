import Landing from "@/features/blog/screens/landing"
import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Trang chủ",
}

const Page = () => <Landing />

export default Page
